

====================================
Problems with traditional Approach
====================================

String name = "Aditya";
int age = 28 ;
int rollno = 101;
String college = "IIT Guwahati";

String name2 = "Aditya";
int age2 = 28 ;
int rollno2 = 101;
String college2 = "IIT Guwahati";

void print(String name , int age , int rollno , String college)
{
    System.out.println(...);
}

Problems
============

1) Need a lot of independent variable to represent one student.
2) If I pass the student to a function I need to pass all 4 variables.
3) I need to create new set of variables for new student.
4) No authority over data.


Object Oriented Programming
============================

Real world ki mimic karte hai oop mein.

Objects are being created in Heap memory.

Student s1;        // Reference variable declaration
s1 = new Student(); // Object creation and reference assignment
s1 is a reference variable created in stack memory (if it's a local variable).
new Student() creates a Student object in heap memory.
The new keyword returns the address (reference) of the created object.
That address is stored in s1.
s1 does not store the object itself; it only stores the reference (memory address) of the object.

new -> dynamic memory allocation , dynamic means runtime mein 

---> compile time [static]
---> Run time [dynamic]

public class practice{
    public static void main(String[] args) {
        Student s1 = new Student();
        Student s2 = new Student();

        s1.name = "Aditya";
        s1.age = 28;
        s1.rollNumber = 101;
        s1.college = "IIT Guwahati";

        s2.name = "Rohit";
        s2.age = 28;
        s2.rollNumber = 102;
        s2.college = "IIT Guwahati";

        s1.markAttendance();
        s2.markAttendance();

        s1.print();
        s2.print();
    }
}

class Student{
    String name;
    int age;
    int rollNumber;
    String college;

    void markAttendance(){
        System.out.println("Attendance marked by " + name);
    }

    void print(){
        System.out.println(name + "," + age + "," + rollNumber + "," + college);
    }

}


========================
Constructors in Java
========================

public class practice{
    public static void main(String[] args) {
        Student s1 = new Student();

        //default values
        System.out.println(s1.name);
        System.out.println(s1.age);
        System.out.println(s1.rollno);
        System.out.println(s1.college);
    }
}

class Student{
    String name;
    int age;
    int rollno;
    String college;
}


"Object heap memory ke andar banta hai. Jo variables us object ka part hote hain (non-static variables), 
woh bhi object ke saath heap memory mein store hote hain. Inhi variables ko instance variables kehte hain."

In instance variables ke pass hoti hai default value, Agar koi value humne assign nhi ki toh  default  values assign hojayengi.

Har data type ke liye default values alag hoti hai

/*
Integer -> 0
floating -> 0.0
Boolean -> false
String -> null
*/

Java mein hum jab bhi koi object declare karte hai aur us object ka jo reference variable hota hai uski value hum dete hai null.
String humara normal data type nhi hai , primitive data type nhi hai , String ek non-primitive data type hai , ye classes and objects ka part hoga , 
Aur String ek class hai aur uski hum object bhi banate honge.

toh String college; -> college ek reference variable hai , is reference variable mein humne kuch dala hi nhi , isliye bydefault null store hogaya.

imp: Local variables hai no default values. Error-> int x;
                                                    System.out.println(x); err-> local variable x is not initialized



Constructors -> To create an Object

Rules of Constructor
====================

1) Same name as class.
2) No return type , not even void
3) Automatically called during object creation.
4) Used to initialize an object.
5) It can also be Overloaded.

ye hai default Constructor :

Student() {
//empty           --> ye java khud se banata hai jab aap nhi banate ho constructor
}

Q) Can we call our Constructor manually? 

Ans: No ya toh this(name,age) karke call karenge ya jab object banega tab call hoga Student s1 = new Student();
     but normal function ke tarah call nhi kar sakte 


Q) ye jo new keyword hai ye dynamically(run time) pe memory ko allocate karta hai heap memory mein lekin aisa bhi toh hosakta hai ki runtime pe heap memory mein space hi na mile?

Ans: Bilkul aisa hosakta hai ki heap memory humari pehle se hi full ho, hume space hi na mile is object ko allocate karne ka . Us case mein hume ek runtime exception milega , jab humare pass enough spcae nhi hoga heap memory mein.


=================
Object Deep Dive
=================

Student s1 = new Student();

s1 in stack memory takes 4 bytes or 8 bytes depends on JVM , but generally 4 bytes

Object Size: 1. Header Size/ Object Headers
             2. Exact fields
             3. Padding   

             Object Headers
                  |
                  |
                Metadata
                /    \
               /      \
            Mark       Class
            Words      Pointer 
 ---> lock                 this keyword
 ---> Synchronization        4 bytes  or 8 bytes
 ---> Garbage collection
      8 bytes  


Exact fields : jo class ke andar exact data hoga uska size.

Padding: jitna bhi data hoga usme aur size add karke koshish karega 8 bytes ke multiple mein karne ka. good for CPU

Call by Value vs call by Reference
===================================

//call by value
public class practice{
    public static void main(String[] args) {
        int x = 4;
        int y = 5;

        System.out.println(x + "," + y);

        addTen(x,y);

        System.out.println(x +"," + y);
    }
    static void addTen(int x , int y)
    {
        x = x+10;
        y = y+10;
    }
}

o/p: 4,5
     4,5


//call by reference ---> There is no call by reference in java.

public class practice{
    public static void main(String[] args) {
        Random r1 = new Random(4,5);

        System.out.println(r1.x+" ,"+r1.y);

        addTen(r1);

        System.out.println(r1.x+" ,"+r1.y);

    }
    static void addTen(Random r){
        r.x = r.x+10;
        r.y = r.y+10;
    }
}

class Random{
    int x;
    int y;

    Random(int x, int y)
    {
        this.x = x;
        this.y = y;
    }
}

Call by Value — Notes

Call by Value is a method of passing arguments to a function where a copy of the actual value is passed to the function.

Key Points
The function receives a copy of the original variable.
Changes made to the parameter inside the function do not affect the original variable.
The actual variable and function parameter have separate memory locations.

Aur java mein call by value ka sirf concept hai , call by reference ka nhi 
Aur jo woh code work kar raha tha woh bhi call by value hi kar raha hai, call by reference nhi

Second case mein kya hua ki r1 ke andar jo address tha object ka woh r , ek naya reference varianle bana aur uske andar store hogaya
toh isliye changes same object mein hue but hua call by value hi , isliye r1 ne same address pe value increment kardi.


so Java is call by value always not call by reference but non-primitives ke case mein same object ka address copy hojata hai alag variables mein toh taste milta hai call by reference.


